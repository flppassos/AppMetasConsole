const start = () => {

    while(true) {
        
        let op = "1 - Cadastrar";

        switch (op) {
            case "1 - Cadastrar":
                console.log('Cadastrar');
                break
        }

        return
    }
} 

start();